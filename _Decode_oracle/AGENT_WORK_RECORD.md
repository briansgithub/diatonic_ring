# Agent Work Record — MOVED

This handoff document has been replaced by the **single source of truth** for the Hooktheory chord-decode workflow:

## → [`ORACLE_GUIDE/`](../ORACLE_GUIDE/README.md)

Start at [`ORACLE_GUIDE/README.md`](../ORACLE_GUIDE/README.md), then read `01_setup.md` → `05_validate_and_log.md` and `reference.md` in order. A new agent with zero context can reproduce the entire loop (download ground truth → build the chord DB → find/fix engine discrepancies → validate → log) from those docs.

The append-only numbered fix history remains here: [`DECODE_FIX_LOG.md`](./DECODE_FIX_LOG.md). Deferred/non-engine issues: [`REMAINING_FAILURES.md`](./REMAINING_FAILURES.md).
